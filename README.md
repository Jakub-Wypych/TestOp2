# TestOp2

## Uruchomienie

Do uruchomienia testów potrzebna jest działająca baza danych:

```bash
TestOp2\ProductApi> dotnet run
```

Oraz uruchomiona aplikacja na porcie 7086:

```bash
TestOp2\BLZR> dotnet run --urls "https://localhost:7086"
```
